Family-wide search field for the identity band; collapses to an icon on mobile.

```jsx
<SearchField placeholder="Search the Idea Factory" />
<SearchField compact />   /* mobile: icon only */
```

Visual element only in mockups — the real search carries over from the existing Mtech/Idea Factory site family. Use `compact` in the mobile chrome where search collapses to an icon.
